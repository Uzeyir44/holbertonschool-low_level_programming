#ifndef MAIN_H
#define MAIN_H

int _putchar(char c);  /* prototype for the provided function */
int sum_them_all(const unsigned int n, ...); /* sums all the variables assigned to the function and returns its */

#endif